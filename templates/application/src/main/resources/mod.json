{
	"main" : "${ORGANISATION}.${APPNAME.toLowerCase()}.${APPNAME}",
	"port" : ${PORT},
	"app-name" : "${APPNAME}",
	"app-address" : "/${APPNAME.toLowerCase()}",
	"app-icon" : "${APPNAME.toLowerCase()}-large",
	"app-displayName" : "${APPNAME}",
	"mode": "dev",
	"entcore.port" : 8090,
	"auto-redeploy": false
}
